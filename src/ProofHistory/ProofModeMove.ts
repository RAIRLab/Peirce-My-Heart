/**
 * @file Contains the enum for applied moves in Proof Mode.
 *
 * @author Ryan R
 */

export enum ProofModeMove {
    CLEAR,
    DC_INSERT,
    DC_DELETE,
    MOVE_SINGLE,
    MOVE_MULTI,
    ITERATION,
    DEITERATION,
    INSERTION,
    ERASURE,
    RESIZE,
    PASTE_GRAPH,
}
