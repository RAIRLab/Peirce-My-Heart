/**
 * @file Contains the enum for applied moves in Draw Mode.
 *
 * @author Ryan R
 */

export enum DrawModeMove {
    CLEAR,
    DRAW_ATOM,
    DRAW_CUT,
    MOVE_SINGLE,
    MOVE_MULTI,
    COPY_SINGLE,
    COPY_MULTI,
    DELETE_SINGLE,
    DELETE_MULTI,
    RESIZE,
    COPY_GRAPH,
}
